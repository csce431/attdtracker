<!-- Link to new if now alreayd in database, otherwise, link to UIN_new -->

<div class="page-content">

<h1>Attendance</h1> 
<p>
    <%= link_to_if(!@tookattendance, "Take Attendance!", new_course_day_path) do
     link_to("Take Attendance!", new_course_day_card_path(@course, @day))
   end
    %>
</p>

<p>
    Current Time: <em><%= @time %></em>
    <br>
</p>

<% @classday %>

<table>
    <tr>
        <th>Name</th>
        <% @days.each do |day| %>
            <th><%= day.classday %></th>
        <% end %>
    </tr>
    <% @cards.each do |card| %>
        <tr>
            <td><%= card.firstname %>  <%= card.lastname %><td>
                 <% if @time == @day.classday %>
                <% @days.each do |day| %>
                    <% if card.day_ids == day.id %>
                        <%= "HERE " %>
                    <% else %>                        <%= "ABSENT" %>
                    <% end %>
                <% end %>
            <% end %>    
        </tr>
    <% end %>
</table>

</div>