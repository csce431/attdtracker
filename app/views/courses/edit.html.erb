<div class="page-content">

<h1>Edit Course</h1>

<%= render 'form' %>

<%= link_to ('<i class="fas fa-trash" title="Delete Course"></i>').html_safe, teacher_course_path(@teacher, @course),
              method: :delete,
              data: { confirm: 'Are you sure you want to remove this course?' } %><br><br>
    
<%= link_to 'Back', teacher_course_path(@teacher, @course) %>

</div>