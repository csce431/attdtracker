<%= form_with(model: @course, local: true) do |form| %>
 
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@course.errors.count, "error") %> prohibited
        this course from being saved:
      </h2>
      <ul>
        <% @course.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div><%= form.text_field :name, placeholder: "Department"  %></div>
  <div><%= form.text_field :number, placeholder: "Course Number"  %></div>
  <div><%= form.text_field :section, placeholder: "Section" %></div>
  <div><%= form.text_field :year, placeholder: "Year" %></div>
  <br>

  <p>
    <%= form.label "Semester" %>
    <%= form.select(:season, options_for_select([["Fall","Fall"], ["Spring", "Spring"], ["Summer", "Summer"]])) %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>

<% end %>