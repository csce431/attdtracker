<% if session[:token] == @user.google_refresh_token%> <%# is this the right thing I should be checking for authenticity?%>
    <%= @student.fname%>
    
    <%= render 'form'%>

<% else %>
    <h1>Incorrect Credentials!</h1>
<% end %>

